import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Universal One School - Revolutionary VR Educational Gaming System',
  description: 'Complete state-compliant curriculum in 90 minutes daily with self-hosted AI, supporting all learning types across US, Austrian, and Mexican educational standards.',
  keywords: 'education, vr gaming, ai, neurodivergent, charter school, personalized learning, universal one school',
  authors: [{ name: 'Shatzii Education Systems' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#3b82f6" />
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  );
}
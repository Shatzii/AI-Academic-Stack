[build]
  # Build command for the frontend
  command = "npm run build:netlify"
  # Directory to publish (Vite's dist folder)
  publish = "dist"

[build.environment]
  # Use latest LTS Node version for better performance
  NODE_VERSION = "20"
  # Build environment variables
  NODE_ENV = "production"
  # Vite specific environment variables
  VITE_APP_ENV = "production"

# Redirect API calls to backend service
[[redirects]]
  from = "/api/*"
  to = "https://your-django-backend.herokuapp.com/api/:splat"
  status = 200
  force = true

# Handle client-side routing for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers for all routes
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://www.googletagmanager.com https://www.google-analytics.com data:; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://fonts.googleapis.com https://fonts.gstatic.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https: blob:; connect-src 'self' https://api.openedtex.com wss://api.openedtex.com https://cdn.jsdelivr.net https://www.google-analytics.com; frame-src 'none';"

# Cache static assets aggressively
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache other static files
[[headers]]
  for = "/favicon.ico"
  [headers.values]
    Cache-Control = "public, max-age=86400"

[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# MIME type headers for JavaScript modules
[[headers]]
  for = "/assets/*.js"
  [headers.values]
    Content-Type = "application/javascript"
    Cache-Control = "public, max-age=31536000, immutable"

# MIME type headers for CSS modules
[[headers]]
  for = "/assets/*.css"
    [headers.values]
    Content-Type = "text/css"
    Cache-Control = "public, max-age=31536000, immutable"

# MIME type headers for images
[[headers]]
  for = "/assets/*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/*.jpg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/*.jpeg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/*.webp"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/*.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Prevent direct access to source files
[[headers]]
  for = "/*.jsx"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"

[[headers]]
  for = "/*.js.map"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"

# API routes headers
[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
    Cache-Control = "no-cache, no-store, must-revalidate"
  [headers.values]
    Content-Type = "application/javascript"

# MIME type headers for all JS files in dist
[[headers]]
  for = "/assets/js/*.js"
  [headers.values]
    Content-Type = "application/javascript"

# MIME type headers for all CSS files in dist
[[headers]]
  for = "/assets/css/*.css"
  [headers.values]
    Content-Type = "text/css"

# MIME type headers for vendor files
[[headers]]
  for = "/assets/js/vendor-*.js"
  [headers.values]
    Content-Type = "application/javascript"

# MIME type headers for React vendor files
[[headers]]
  for = "/assets/js/react-vendor-*.js"
  [headers.values]
    Content-Type = "application/javascript"

# Catch-all MIME type headers for all assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"

# Cache static assets aggressively
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache other static files
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"

# Netlify Functions configuration (if using serverless functions)
[functions]
  directory = "netlify/functions"

# Build hooks for continuous deployment
[build.hooks]
  prebuild = "echo 'Starting build process...'"

# Environment variables for production
[context.production.environment]
  VITE_API_URL = "https://your-django-backend.herokuapp.com/api"
  VITE_APP_ENV = "production"

# Branch-specific configurations
[context.branch-deploy.environment]
  VITE_API_URL = "https://staging-api.openedtex.com/api"
  VITE_APP_ENV = "staging"

[context.deploy-preview.environment]
  VITE_API_URL = "https://dev-api.openedtex.com/api"
  VITE_APP_ENV = "development"

# Secrets scanning configuration
[secrets_scan]
  # Disable secrets scanning for this build
  # VITE_ variables are public client-side configuration
  enabled = false
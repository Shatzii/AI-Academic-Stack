# AI-Academic-Stack - Complete Setup & Deployment Guide

## ✅ What's Been Completed

Your AI-Academic-Stack application is now **fully configured** with automated setup scripts and comprehensive documentation.

### Files Created:
1. ✅ `.env` - Environment configuration with auto-generated secrets
2. ✅ `.env.setup-guide.md` - Detailed setup instructions
3. ✅ `scripts/setup-database.sh` - Interactive database setup helper
4. ✅ `scripts/setup-api-keys.sh` - API keys configuration helper
5. ✅ `scripts/migrate-database.sh` - Database migration runner
6. ✅ `scripts/build-app.sh` - Complete build automation
7. ✅ Updated `package.json` with convenient npm scripts

### Dependencies:
- ✅ All npm packages installed (1029 packages)
- ✅ No critical vulnerabilities

---

## 🚀 Quick Start Guide

### Option 1: Interactive Setup (Recommended)

```bash
# Run the complete setup wizard
npm run setup:database  # Get database connection string
npm run setup:api-keys  # Get API keys
```

### Option 2: Manual Setup

1. **Get a Database (Required)**
   - Visit: https://neon.tech
   - Create free account (no credit card)
   - Create new PostgreSQL database
   - Copy connection string
   - Update `DATABASE_URL` in `.env`

2. **Get Anthropic API Key (Required)**
   - Visit: https://console.anthropic.com
   - Sign up ($5 free credit)
   - Create API key
   - Update `ANTHROPIC_API_KEY` in `.env`

3. **Run Migrations**
   ```bash
   npm run db:push
   ```

4. **Build Application**
   ```bash
   npm run build
   ```

5. **Start Server**
   ```bash
   npm start
   ```

---

## 📋 Available NPM Scripts

### Setup Scripts
- `npm run setup` - Complete setup wizard (all steps)
- `npm run setup:database` - Database setup helper
- `npm run setup:api-keys` - API keys setup helper

### Development
- `npm run dev` - Start development server
- `npm run check` - TypeScript type checking

### Database
- `npm run db:push` - Push schema to database (create tables)

### Production
- `npm run build` - Build for production
- `npm start` - Start production server

---

## 🔧 Environment Variables Reference

### Required (Minimum to Run)
```env
DATABASE_URL=postgresql://...           # PostgreSQL connection string
ANTHROPIC_API_KEY=sk-ant-...           # Anthropic Claude API key
SESSION_SECRET=<auto-generated>         # Session encryption (✅ done)
JWT_SECRET=<auto-generated>             # JWT token signing (✅ done)
```

### Optional (Enhanced Features)
```env
OPENAI_API_KEY=sk-...                  # OpenAI GPT models
PERPLEXITY_API_KEY=pplx-...            # Perplexity AI research
SENDGRID_API_KEY=SG....                # Email notifications
STRIPE_SECRET_KEY=sk_test_...          # Payment processing
VITE_STRIPE_PUBLIC_KEY=pk_test_...     # Stripe frontend
```

---

## 🗄️ Database Options

### Free Tier Providers (No Credit Card)

| Provider | Storage | Features | Link |
|----------|---------|----------|------|
| **Neon** (Recommended) | 0.5 GB | Serverless, Auto-scaling | https://neon.tech |
| Supabase | 500 MB | Built-in Auth, Storage | https://supabase.com |
| Railway | $5 credit | Easy deployment | https://railway.app |
| ElephantSQL | 20 MB | Simple setup | https://elephantsql.com |

---

## 🤖 AI API Keys

### Anthropic (Required)
- **Cost**: $5 free credit
- **Models**: Claude 3.5 Sonnet, Claude 3 Opus
- **Get it**: https://console.anthropic.com
- **Why needed**: Powers all AI tutoring, content generation, and adaptive learning

### OpenAI (Optional)
- **Cost**: Free trial available
- **Models**: GPT-4, GPT-3.5
- **Get it**: https://platform.openai.com

### Perplexity (Optional)
- **Cost**: Various plans
- **Use case**: Research and knowledge retrieval
- **Get it**: https://www.perplexity.ai

---

## 📦 What's Included

### Frontend Features
- ✅ Next.js 15 with App Router
- ✅ Tailwind CSS + Shadcn UI components
- ✅ Responsive design (mobile-first)
- ✅ Dark mode support
- ✅ Multiple school themes (Primary, Secondary, Law, Language, Sports)

### Backend Features
- ✅ Express.js API server
- ✅ PostgreSQL with Drizzle ORM
- ✅ Passport.js authentication
- ✅ Session management
- ✅ JWT token support
- ✅ Rate limiting
- ✅ Security headers (Helmet)

### AI Capabilities
- ✅ AI Tutor (personalized learning)
- ✅ Content Generator (curriculum creation)
- ✅ Adaptive Learning System
- ✅ Learning Style Assessment
- ✅ Neurodiversity Support

### Schools & Programs
- ✅ SuperHero Academy (Primary School)
- ✅ Secondary School (with Stage Prep)
- ✅ Law School
- ✅ Language School
- ✅ Sports Science Academy

---

## 🔍 Verification Checklist

Before first run, verify:

- [ ] `.env` file exists
- [ ] `DATABASE_URL` updated (no "placeholder")
- [ ] `ANTHROPIC_API_KEY` updated (no "placeholder")
- [ ] `SESSION_SECRET` is set (auto-generated ✅)
- [ ] `JWT_SECRET` is set (auto-generated ✅)
- [ ] Database migrations run successfully
- [ ] Build completed without errors
- [ ] Port 5000 is available

---

## 🚨 Troubleshooting

### "DATABASE_URL is not set"
- Check `.env` file exists
- Ensure no typos in variable name
- Verify connection string format

### "Database connection failed"
- Test connection string in database client
- Check firewall/network settings
- Verify database service is running

### "ANTHROPIC_API_KEY invalid"
- Check key starts with `sk-ant-`
- Verify key is active in console
- Check for extra spaces/quotes

### Build errors
```bash
# Clear cache and rebuild
rm -rf node_modules dist
npm install
npm run build
```

---

## 📊 System Requirements

- **Node.js**: 18+ (LTS recommended)
- **RAM**: 2GB minimum, 4GB recommended
- **Storage**: 500MB for dependencies + database
- **OS**: Linux, macOS, or Windows (WSL2)

---

## 🔒 Security Notes

✅ **Completed for you:**
- Session secrets auto-generated (64 chars, cryptographically secure)
- JWT secrets auto-generated (64 chars, cryptographically secure)
- Environment variables properly isolated

⚠️ **You must do:**
- Never commit `.env` to version control (already in `.gitignore`)
- Use strong passwords for database users
- Rotate API keys regularly
- Enable 2FA on all service accounts

---

## 🎯 Next Steps After Setup

1. **Test the Application**
   ```bash
   npm run dev
   # Visit: http://localhost:5000
   ```

2. **Create First User**
   - Navigate to `/auth/register`
   - Create admin account
   - Set up school programs

3. **Configure Schools**
   - Access admin dashboard
   - Customize school themes
   - Upload branding assets

4. **Test AI Features**
   - Try AI Tutor
   - Generate curriculum content
   - Test adaptive learning

5. **Deploy to Production**
   - Update `NODE_ENV=production` in `.env`
   - Set up SSL/HTTPS
   - Configure domain name
   - Deploy to Vercel/Railway/Render

---

## 📚 Additional Resources

- **Full Documentation**: See individual `.md` files in project root
- **Database Guide**: `.env.setup-guide.md`
- **Deployment Guide**: `deployment-ready.md`
- **API Integration**: `AI-ENGINE-INTEGRATION.md`

---

## 💡 Development Tips

```bash
# Development mode (hot reload)
npm run dev

# Type checking
npm run check

# Database management
npm run db:push    # Apply schema changes

# Production testing
npm run build
npm start
```

---

## 🆘 Getting Help

1. Check `.env.setup-guide.md` for detailed instructions
2. Run setup scripts: `npm run setup:database` or `npm run setup:api-keys`
3. Review error messages carefully
4. Check logs in terminal for specific errors

---

## ✨ You're All Set!

The application is **ready to run** once you:
1. Update `DATABASE_URL` with real database
2. Update `ANTHROPIC_API_KEY` with real API key
3. Run `npm run db:push`
4. Run `npm run build`
5. Run `npm start`

**Minimum time to deploy**: 10-15 minutes
**Recommended time for full setup**: 30 minutes

Happy building! 🚀

